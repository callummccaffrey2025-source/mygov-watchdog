supabase>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
# brotli is required for Accept-Encoding: br used by scraper_etiquette.py.
# Without it urllib3 can't decompress brotli responses and returns garbled bytes.
brotli>=1.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
anthropic>=0.40.0
feedparser>=6.0.0
# Optional: embedding-based clustering (used by cluster_with_embeddings.py)
# Uses OpenAI embeddings + Pinecone via HTTP directly (no SDKs needed)
