#!/usr/bin/env bash
. "$(dirname "$0")/_/husky.sh"
if git diff --cached --name-only | grep -E '(^|/)\.env(\..*)?$' >/dev/null; then
  echo "✗ Refusing to commit .env* files. Use .env.example for sharable config." >&2
  exit 1
fi
